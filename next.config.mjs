/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'upload.wikimedia.org', // For social media logos
        'images.unsplash.com',  // For Unsplash images
      ],
    },
  };
  
  export default nextConfig;
  